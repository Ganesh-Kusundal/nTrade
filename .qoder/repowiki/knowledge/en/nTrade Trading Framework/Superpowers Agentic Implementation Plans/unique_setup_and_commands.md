Every plan mandates running tests via `.venv/bin/python -m pytest -q` from `/Users/apple/Downloads/nTrade`; plans are executed by agentic workers using the `superpowers:subagent-driven-development` or `superpowers:executing-plans` sub-skills; kanban state is synced with `python3 /Users/apple/.agents/skills/kanban.cli/scripts/kanban.py update` after completing tasks; git commits use short `<ID> subject` style matching `git log --oneline`.