Pure Python with `dataclasses` (all market value objects are `@dataclass(frozen=True)`), `enum.Enum` for typed constants, `functools.cached_property` for lazy capability resolution, and `typing.TYPE_CHECKING` for circular-import avoidance. Analytics uses only the standard library `math` module (no numpy/pandas dependency in the core). Optional pandas DataFrames are imported lazily inside capability methods.