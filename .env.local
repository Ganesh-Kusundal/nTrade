# Local environment overrides — NOT committed with real secrets.
# Copy values from `.env` here to override them on this machine without
# touching the shared `.env`. This file IS tracked; keep it free of secrets.

# Example:
# DHAN_CLIENT_ID=
# DHAN_ACCESS_TOKEN=
# DHAN_ENVIRONMENT=
